version: '3.7'

services:
    app:
        image: python:3.7-alpine
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '8000:8000'
        volumes:
            - ./app:/app
        command: >
            sh -C 'python manage.py runserver 0.0.0.0:8000'